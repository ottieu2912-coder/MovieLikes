Notice: This project uses the requests library of python and requires the users to enter the virtual environment in order to use it.
Also, the API in this project might be outdated. In that case, create a new Omdb api for search and replaces the old API with it in the home function in the views file.
